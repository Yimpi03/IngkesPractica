// modules/memory/memory-routing-module.ts
import { Routes } from '@angular/router';
import { Memory } from './memory/memory';

export const memoryRoutes: Routes = [
  { path: '', component: Memory }
];